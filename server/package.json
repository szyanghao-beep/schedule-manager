{
  "name": "schedule-sync-server",
  "version": "1.0.0",
  "description": "日程管理同步后端：认证(JWT) + 记录级增量同步 + 服务端时间仲裁（LWW）",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2"
  }
}
